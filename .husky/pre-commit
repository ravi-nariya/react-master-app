#!/bin/sh
# Run lint-staged (ESLint --fix + TypeScript type-check on staged files)
npx lint-staged

# Fail fast on known npm vulnerabilities at moderate severity or above
npm audit --audit-level=moderate
