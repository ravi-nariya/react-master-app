# Fail install if any dependency has a vulnerability at moderate level or above
audit-level=moderate

# Prevent accidental publish of private package
//registry.npmjs.org/:_authToken=${NPM_TOKEN}

# Enforce exact version resolution to prevent unexpected upgrades
save-exact=true
